

export default function Deals_and_offers_item(image,name,discount){

      return(
            <div className="deals_and_offers_item">
                        <img className="item-img " src={product.image} alt=""/>
                        <div className="item-name">{prodcut.name}</div>
                        <div className="item-discount">{product.discount}</div>  
                  </div>
      )
};